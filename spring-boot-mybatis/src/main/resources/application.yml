spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sql_study?serverTimezone=GMT%2B8

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    cache-enabled: true